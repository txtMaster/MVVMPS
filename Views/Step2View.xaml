<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <StackPanel Margin="20">

        <TextBlock Text="Paso 2" Background="Red"/>

        <TextBox
        Text="{Binding Config.SystemDirname, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
        Foreground="Red"/>

    </StackPanel>

</UserControl>